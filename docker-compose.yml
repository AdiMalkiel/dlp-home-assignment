services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: dlp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d dlp']
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: .
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/dlp
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
